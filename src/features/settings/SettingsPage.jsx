import { FoundationPanel } from '../../components/feedback/FoundationPanel.jsx';

export function SettingsPage({ title }) {
  return (
    <FoundationPanel
      title={title}
      description="The settings route is reserved. Configuration forms and permission-aware behavior are implemented in later phases."
    />
  );
}
